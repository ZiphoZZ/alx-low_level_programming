#include <stdio.h>

/**
 * main - prints numbers from 0 to base 10.
 *
 * Return: 0 as success
 */

int main(void)
{
	int i = 0;

	while (i < 10)
		putchar(i);
		i++;

	putchar('\n');

	return (0);
}
